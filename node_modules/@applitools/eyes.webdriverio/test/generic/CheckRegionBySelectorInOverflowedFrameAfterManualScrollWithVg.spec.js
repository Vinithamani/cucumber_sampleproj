// check region by selector in overflowed frame after manual scroll with vg
const cwd = process.cwd()
const path = require('path')
const assert = require('assert')
const spec = require(path.resolve(cwd, 'src/spec-driver'))
const {testSetup, getTestInfo, getDom} = require('@applitools/sdk-shared')

describe('Coverage Tests - CheckRegionBySelectorInOverflowedFrameAfterManualScrollWithVg', () => {
  let driver, destroyDriver, eyes

  beforeEach(async () => {
    ;[driver, destroyDriver] = await spec.build({browser: 'chrome'})
    eyes = testSetup.getEyes({
      vg: true,
      displayName:
        'check region by selector in overflowed frame after manual scroll with vg',
      baselineName: 'TestCheckRegionInAVeryBigFrameAfterManualSwitchToFrame_VG',
    })
  })

  afterEach(async () => {
    await destroyDriver(driver)
    await eyes.abort()
  })

  it('check region by selector in overflowed frame after manual scroll with vg (@webdriver)', async () => {
    await spec.visit(
      driver,
      'https://applitools.github.io/demo/TestPages/WixLikeTestPage/index.html'
    )
    await eyes.open(
      driver,
      'Eyes Selenium SDK - Special Cases',
      'TestCheckRegionInAVeryBigFrameAfterManualSwitchToFrame_VG',
      {width: 700, height: 460}
    )

    await spec.childContext(
      driver,
      await spec.findElement(driver, '[name="frame1"]')
    )
    await eyes.check({region: 'img'})
    await eyes.close(undefined)
  })
})
